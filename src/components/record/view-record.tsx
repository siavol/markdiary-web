import React, { useState } from 'react'
import { useLoaderData } from 'react-router-dom'

const ViewRecord: React.FunctionComponent = () => {
  return <div>record view to be here</div>
}

export default ViewRecord
